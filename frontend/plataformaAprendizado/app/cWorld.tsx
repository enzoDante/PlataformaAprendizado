import React from 'react';
import { View, Text, ScrollView } from 'react-native';

export default class CWorld extends React.Component {
    render() {
        return(
            <ScrollView>
                <View>
                    <Text>C World Screen</Text>
                </View>
            </ScrollView>
        );
    }
}