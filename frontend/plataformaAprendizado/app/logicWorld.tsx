import React from "react";
import { View, Text, ScrollView } from "react-native";

export default class LogicWorld extends React.Component {
    render() {
        return(
            <ScrollView>
                <View>
                    <Text>Logic World Screen</Text>
                </View>
            </ScrollView>
        );
    }
}