import React from "react";
import { View, Text, ScrollView } from "react-native";

export default class JavaWorld extends React.Component {
    render() {
        return(
            <ScrollView>
                <View>
                    <Text>Java World Screen</Text>
                </View>
            </ScrollView>
        );
    }
}